name: Config Validation

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  validate-upptime-config:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate Upptime config drift
        run: python3 scripts/validate-upptime-config.py
